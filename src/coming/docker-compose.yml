version: "2.1"
services:
  freshrss:
    image: ghcr.io/linuxserver/freshrss
    container_name: freshrss
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
    volumes:
      - ./path/to/data:/config
    ports:
      - 5001:80
      - 5002:433
    restart: unless-stopped